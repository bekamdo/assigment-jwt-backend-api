services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=assignment'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
